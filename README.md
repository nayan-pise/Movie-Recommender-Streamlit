# Movie-Recommender-Streamlit
Movie Recommender (Streamlit) A Python-based Content-Based Movie Recommender System.  It uses Count Vectorizer and Cosine Similarity on metadata (genres, cast, director) to suggest similar movies. The app is deployed using Streamlit for an interactive UI, fetching posters via the TMDB API. Showcases ML deployment skills.
